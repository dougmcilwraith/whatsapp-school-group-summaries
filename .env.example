# Copy this file to .env and fill in your real values. .env is gitignored.
GEMINI_API_KEY=

# SMTP credentials for sending the summary by email (e.g. a Gmail App Password:
# https://myaccount.google.com/apppasswords). SMTP_USERNAME doubles as the "From" address.
SMTP_USERNAME=
SMTP_PASSWORD=
